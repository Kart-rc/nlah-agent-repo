# Raw run: layered rules from mixed evidence

## Metadata

- Invocation date: 2026-07-14 (America/New_York)
- Method: child agent with `fork_turns: "none"`
- Model identifier: unavailable from the child-agent interface
- Working directory: `/private/tmp/claude-baseline-layering`
- Filesystem constraint: read-only evaluation; no edits, creates, or commits

## Wrapper

> Work in /private/tmp/claude-baseline-layering. Read-only evaluation: do not edit or create files and do not commit. Respond to the user request below.

## Prompt

> Please turn these notes into Claude Code project instructions for our monorepo:
>
> - package.json defines `npm run lint`, `npm test`, and `npm run build` at the root.
> - `.github/workflows/ci.yml` runs lint and test for every pull request, and build only on main.
> - `services/payments/README.md` says payment migrations are forward-only and must be tested with `npm run test:migrations` from that service.
> - `services/payments/src/generated/` is generated by `npm run generate` and should not be edited by hand.
> - `apps/storefront/README.md` claims Node 18 is required, but `.nvmrc` and CI both use Node 22.
> - A maintainer review comment on PR #418 says all storefront API calls must go through `apps/storefront/src/api/client.ts` so auth and retry behavior are preserved.
> - During today's debugging, Sam said to skip lint because it was slow on their laptop.
> - The repository also contains unrelated `services/catalog`, `apps/admin`, and `packages/ui` areas.
>
> Create whatever CLAUDE.md or supporting rule files you think future Claude sessions need.

## Fixture mutation audit

Expected before:

```text
(empty directory)
```

Observed after:

```text
(empty directory)
```

## Full response (unabridged)

I’d use layered `CLAUDE.md` files so rules only load where relevant. No files were written because this evaluation is read-only.

`/private/tmp/claude-baseline-layering/CLAUDE.md`

```markdown
# Monorepo Instructions

## Runtime

- Use Node.js 22. `.nvmrc` and CI are authoritative if other documentation disagrees.

## Validation

Run repository-wide checks from the repository root:

- Lint: `npm run lint`
- Tests: `npm test`
- Build: `npm run build`

Lint and tests run in CI for every pull request. Build runs in CI on `main`.

Before finalizing changes, run the checks appropriate to the change. Do not skip a required check because it is slow; if a check cannot be run, report that explicitly.
```

`/private/tmp/claude-baseline-layering/services/payments/CLAUDE.md`

```markdown
# Payments Service Instructions

- Payment database migrations are forward-only. Make corrections with a new forward migration rather than rolling back or rewriting an existing migration.
- Test every payment migration from this directory with `npm run test:migrations`.
- Files under `src/generated/` are generated artifacts. Do not edit them by hand.
- Regenerate `src/generated/` by running `npm run generate` from this directory.
```

`/private/tmp/claude-baseline-layering/apps/storefront/CLAUDE.md`

```markdown
# Storefront Instructions

- Use Node.js 22. The Node 18 requirement in this directory's README is outdated; `.nvmrc` and CI are authoritative.
- Route all storefront API calls through `src/api/client.ts`.
- Do not bypass the shared API client with direct API calls elsewhere, because doing so omits the required authentication and retry behavior.
```

I intentionally excluded Sam’s one-off debugging advice because temporary local convenience should not override root validation policy. I also added no instructions under `services/catalog`, `apps/admin`, or `packages/ui`, since none of the supplied rules apply specifically to those areas.
