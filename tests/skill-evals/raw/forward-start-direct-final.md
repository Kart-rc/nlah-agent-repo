# Direct Start-Codebase Final Raw Evidence

## Run metadata

- Date: 2026-07-14 (America/New_York)
- Evaluator: `/root/forward_validation/forward_start_final`
- Context: fresh child agent with `fork_turns:none`
- Evaluation root: `/private/tmp/claude-context-forward-start-final.CF4Vz0`
- Target: disposable preinstalled fixture with generated caches removed before snapshot
- Skill: installed `.claude/skills/start-codebase/SKILL.md`
- Isolation: no source-repository tests, plans, design, baselines, rubric, prior output, generated bytecode, or expected answer
- Response SHA-256 before this wrapper: `8a3e810475ddfc53e9b86994a096cda57200100c5f31d53941032321798b8046`

## Deterministic fixture construction

The controller used the following repository-relative construction procedure:

```sh
ROOT=/private/tmp/claude-context-forward-start-final.CF4Vz0
mkdir -p "$ROOT/project" "$ROOT/controller"
cp -R .claude/skills/bootstrap-claude-context/assets/project/. "$ROOT/project/"
find "$ROOT/project" -type d -name __pycache__ -prune -exec rm -rf {} +
```

It then applied this exact fixture overlay before snapshotting:

```diff
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@
-# Project Instructions
+# Parcel Desk Instructions

-Keep repository-wide instructions concise. Put specialized guidance in the narrowest relevant `.claude/rules/` file or nested `CLAUDE.md`.
+Use Node.js 22. Run `npm test` and `npm run lint` before completing work.

 Use the installed project skills for codebase onboarding, session reflection, and review-gated learning updates.
--- /dev/null
+++ b/README.md
@@
+# Parcel Desk
+
+Parcel Desk supports internal parcel retry operations. `apps/api` owns retry policy and `apps/web` is the operator console.
+
+Use Node.js 22. Run `npm test` for unit tests and `npm run lint` for lint checks.
--- /dev/null
+++ b/package.json
@@
+{
+  "name": "parcel-desk-start-eval",
+  "private": true,
+  "engines": { "node": "22.x" },
+  "scripts": {
+    "test": "echo unit-tests",
+    "lint": "echo lint-check"
+  }
+}
--- /dev/null
+++ b/.nvmrc
@@
+22
--- /dev/null
+++ b/apps/api/src/retry.ts
@@
+export function shouldRetry(attempt: number): boolean {
+  return attempt < 3;
+}
--- /dev/null
+++ b/apps/api/test/retry.test.ts
@@
+import { shouldRetry } from "../src/retry";
+
+export const retryExamples = [shouldRetry(1), shouldRetry(3)];
--- /dev/null
+++ b/.claude/context/learnings/pending/2026-07-14-review-retry-limit.md
@@
+# Review retry limit guidance
+
+Status: pending
+Target: `.claude/rules/api-retry.md`
+Scope: `apps/api/**`
+Expected benefit: clarify the supported retry limit after repository confirmation.
+
+Pending explicit developer approval. Do not apply from this proposal alone.
```

Snapshot command:

```sh
cp -R "$ROOT/project" "$ROOT/controller/project.before"
```

## Exact evaluator wrapper and prompt

> Work only inside `/private/tmp/claude-context-forward-start-final.CF4Vz0`; do not inspect or reference any other repository or conversation. Treat `/private/tmp/claude-context-forward-start-final.CF4Vz0/project` as the preinstalled target repository. Use the installed start-codebase skill at `/private/tmp/claude-context-forward-start-final.CF4Vz0/project/.claude/skills/start-codebase/SKILL.md` and follow it faithfully for this user request:
>
> I am starting work on the API retry behavior in this unfamiliar repository. Please give me the current developer briefing so I know the applicable instructions, architecture, safe commands, likely change points, pending learnings, unknowns, and best first step.
>
> Return your full user-facing response when complete.

## Full evaluator response

## Current developer briefing

### Worktree and recent changes

Git metadata is unavailable in the target repository, so there is no reliable worktree status, branch, or commit history to report. No files were changed during this briefing.

### Applicable instructions

- Use Node.js 22 and run `npm test` plus `npm run lint` before completing work: [CLAUDE.md](/private/tmp/claude-context-forward-start-final.CF4Vz0/project/CLAUDE.md:3).
- The manifest independently requires Node 22.x: [package.json](/private/tmp/claude-context-forward-start-final.CF4Vz0/project/package.json:4), and [.nvmrc](/private/tmp/claude-context-forward-start-final.CF4Vz0/project/.nvmrc:1) specifies version 22.
- Use the installed project skills for onboarding and review-gated learning updates: [CLAUDE.md](/private/tmp/claude-context-forward-start-final.CF4Vz0/project/CLAUDE.md:5).
- No `AGENTS.md`, `.claude/rules/`, or API-specific instruction file currently exists. No instruction conflicts were found.

### Architecture and entry points

- Parcel Desk is described as an internal parcel-retry system. The README assigns retry policy to `apps/api` and describes `apps/web` as an operator console: [README.md](/private/tmp/claude-context-forward-start-final.CF4Vz0/project/README.md:3). The API ownership is supported by source placement; the web application is not present in this checkout, so that part remains unverified.
- The only API implementation is `shouldRetry(attempt)`, which currently retries whenever `attempt < 3`: [retry.ts](/private/tmp/claude-context-forward-start-final.CF4Vz0/project/apps/api/src/retry.ts:1).
- The only visible dependency direction is API test → retry source, through a direct relative import: [retry.test.ts](/private/tmp/claude-context-forward-start-final.CF4Vz0/project/apps/api/test/retry.test.ts:1).
- That test file exports two examples, but contains no assertions or registered test cases: [retry.test.ts](/private/tmp/claude-context-forward-start-final.CF4Vz0/project/apps/api/test/retry.test.ts:3).
- The package is private, has no declared dependencies, and exposes no build, start, or server script: [package.json](/private/tmp/claude-context-forward-start-final.CF4Vz0/project/package.json:2).
- The codebase map is currently only a placeholder and provides no additional verified architecture: [codebase-map.md](/private/tmp/claude-context-forward-start-final.CF4Vz0/project/.claude/context/codebase-map.md:1).

### Commands and baseline results

- Installed runtime: `node --version` returned `v24.2.0`, which does **not** satisfy the required Node 22 environment.
- `npm test` passed, but its entire implementation is `echo unit-tests`; it ran no tests: [package.json](/private/tmp/claude-context-forward-start-final.CF4Vz0/project/package.json:6).
- `npm run lint` passed, but likewise only executes `echo lint-check`: [package.json](/private/tmp/claude-context-forward-start-final.CF4Vz0/project/package.json:7).
- No dependency installation, network access, migrations, fix commands, secrets, or external systems were used.
- Before development, switch to an already-installed Node 22 runtime. Do not treat the current test/lint results as behavioral validation.

### Likely change points

- Retry-policy implementation: [apps/api/src/retry.ts](/private/tmp/claude-context-forward-start-final.CF4Vz0/project/apps/api/src/retry.ts:1).
- Boundary and behavior coverage: [apps/api/test/retry.test.ts](/private/tmp/claude-context-forward-start-final.CF4Vz0/project/apps/api/test/retry.test.ts:1).
- Test/lint infrastructure, if real executable checks are in scope: [package.json](/private/tmp/claude-context-forward-start-final.CF4Vz0/project/package.json:5).

Current behavior accepts all numeric values below 3—including zero, negatives, and fractions—and rejects 3 or greater. Whether that is intentional is unknown.

### Pending learnings

One pending, unapplied proposal requests confirmation of the supported retry limit before creating `.claude/rules/api-retry.md`: [2026-07-14-review-retry-limit.md](/private/tmp/claude-context-forward-start-final.CF4Vz0/project/.claude/context/learnings/pending/2026-07-14-review-retry-limit.md:1). It explicitly requires developer approval and must not be treated as active guidance.

There are no unprocessed learning receipts to evaluate.

### Unknowns and best first step

Unknowns include:

- Whether `attempt` is zero- or one-based.
- Whether “3” means three retries or three total attempts.
- Which failures are retryable and whether status codes, idempotency, delay, jitter, or `Retry-After` matter.
- Who calls `shouldRetry` in the real application.
- Whether missing API/web/runtime files are intentional.
- Which test and lint tools the project intends to use.

The best first step is to confirm the retry contract—especially attempt numbering and retry-versus-total-attempt limits—then establish an executable boundary test for attempts around the limit before changing [retry.ts](/private/tmp/claude-context-forward-start-final.CF4Vz0/project/apps/api/src/retry.ts:1). The existing pending proposal reinforces that the limit should not be codified until repository or owner confirmation.

## Before manifest

Manifest and diff commands:

```sh
find "$ROOT/controller/project.before" -type f -exec shasum -a 256 {} + | sort
find "$ROOT/project" -type f -exec shasum -a 256 {} + | sort
diff -ruN "$ROOT/controller/project.before" "$ROOT/project"
```

```text
09fcf8a4ef66457f06aa8885245e4b6533b56c3f59dfc8aa976ed11445794e1c  CLAUDE.md
7cdc73c3fb538f5d101df86bfcc3c73b3e2b2211ad18fbc5604a587a1d3651a3  .claude/settings.fragment.json
7d13512461dbc579b5818162188a818a52e0f256eeb8af80ed11273231ddf197  .claude/hooks/session-start.py
8bed96582c3f31c420066b176c98d563df2551f016d3a583e48502e55fb2d54b  README.md
8caf47578b0cdf7ffbe8db0d8e4310e3225262f84a0524e72ce6c801dd073803  apps/api/test/retry.test.ts
90069c9bac9c242e1b789b60f64afe124a9cf6139516ab14af4deb2ad359e341  .claude/skills/finish-session/SKILL.md
91e975a2b208c9738425fe22431b1018c7567d4ec12389ae2721bd8e02c3ce99  .claude/context/codebase-map.md
a1f9119e659e72e8d3663fe729131ae1f0965c669e5dc6a14ca9f9543328c7cd  apps/api/src/retry.ts
a687a6272b3939ea4a676adc6c3f82e803e9c04a72315c359b98905c37db9d06  .claude/context/learnings/.gitignore
cdcff3c395300977fe4fba2cfe4f4990f2f28fa7efdfb99d76d841f99f61cee7  .claude/context/learnings/pending/2026-07-14-review-retry-limit.md
d559e575ce96076b6653c8dd3f3e776d320648cd010dedba08029afca2c4ad0b  package.json
e531ecb035c6d935468382396ca6cce14034d211f3bb10402439893fa01a4227  .claude/skills/start-codebase/SKILL.md
efc9e77a1e6e23a514d59896917a12c7bda978a2dd08e26c00b6d87c717f6116  .claude/skills/review-learnings/SKILL.md
f14b4987904bcb5814e4459a057ed4d20f58a633152288a761214dcd28780b56  .nvmrc
f7e3d6928686a32a14ed31389bd8bf5dc978b04103060c649e31f740b97e61ec  .claude/hooks/session-end.py
```

## After manifest and diff

The after manifest is byte-for-byte identical to the before manifest. `diff -ruN` exited 0 with no output; the direct start briefing did not mutate the fixture.
