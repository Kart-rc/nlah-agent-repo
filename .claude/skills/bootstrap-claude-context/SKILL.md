---
name: bootstrap-claude-context
description: Use when installing or validating the stable Claude Code context scaffold in a repository.
---

# Bootstrap Claude Context

Run `scripts/install.py --help` for the deterministic installer interface.

Do not derive or update repository rules from this installer alone. The codebase analysis and review-gated learning workflows are authored separately.
